# Оптимизация стоимости промптов

## ✅ РЕАЛИЗОВАНО (2026-01-22)

Этот документ описывает реализованные оптимизации промптов для снижения стоимости поиска новостей.

---

## Проблема

**До оптимизации:**
- Промпты были многословными (~434 токена input/ресурс)
- JSON примеры с описаниями полей (~100 токенов избыточно)
- LLM генерировал source_url (~30 токенов output/новость)
- Summary требовал "1-2 абзаца" (~200-350 токенов/новость)
- System промпты были избыточными (~40 токенов)

**Стоимость (Grok):** ~$0.13 за 220 ресурсов = **~$3.90/месяц**

---

## Реализованные оптимизации

### 1️⃣ Сокращение промптов (✅ реализовано)

**БЫЛО (английский промпт):**
```
Find all news on the website {url} ({name}) for the period from {start_date} to {end_date} inclusive.

Use web search to find news. Look for all articles, publications, press releases, news published on the website for the specified period. For each found news item, find the title, news text (1-2 paragraphs) and source link.

**IMPORTANT: Translate all news to Russian. Return only Russian text.**
```

**СТАЛО:**
```
Find news on {url} ({name}), {start_date} to {end_date}.

Use web search. For each: title, summary (1 paragraph). **Translate to Russian only.**
```

**Экономия:** ~120 токенов/промпт × 220 = **26,400 токенов input** = **$0.0026/запрос**

---

### 2️⃣ Минималистичный JSON формат (✅ реализовано)

**БЫЛО:**
```json
{
  "news": [
    {
      "title": "Заголовок новости на русском",
      "summary": "Текст новости на русском языке (1-2 абзаца). Пиши новость напрямую, как журналист, от третьего лица.",
      "source_url": "https://example.com/news/article"
    }
  ]
}

If no news found, return: {"news": []}

Return ONLY JSON, without additional comments or explanations.
```

**СТАЛО:**
```json
JSON:
{"news": [{"title": "...", "summary": "..."}]}

Empty: {"news": []}
```

**Экономия:** ~80 токенов/промпт × 220 = **17,600 токенов input** = **$0.0018/запрос**

---

### 3️⃣ Оптимизация system промптов (✅ реализовано)

**БЫЛО (OpenAI):**
```
Ты - эксперт по поиску новостей. Возвращай ответ в формате JSON.
```

**СТАЛО:**
```
News search. Return JSON: {"news": [{"title": "...", "summary": "..."}]}
```

**БЫЛО (Grok):**
```
Используй веб-поиск для поиска новостей. Возвращай ответ в формате JSON.
```

**СТАЛО:**
```
News search with web. JSON: {"news": [{"title": "...", "summary": "..."}]}
```

**БЫЛО (Anthropic):**
```
Ты - помощник для поиска новостей. Верни результат ТОЛЬКО в формате JSON.
```

**СТАЛО:**
```
News search. JSON: {"news": [{"title": "...", "summary": "..."}]}
```

**Экономия:** ~30 токенов/промпт × 220 = **6,600 токенов input** = **$0.0007/запрос**

---

### 4️⃣ Сокращение summary (✅ реализовано)

**БЫЛО:**
```
"summary": "Текст новости на русском языке (1-2 абзаца)"
```

**СТАЛО:**
```
"summary (1 paragraph)"
```

**Экономия:** ~40% output токенов = **~78,000 токенов output** = **$0.023/запрос**

---

### 5️⃣ Удаление source_url из output (✅ реализовано)

**БЫЛО:**
- LLM генерировал source_url (~30 токенов/новость)
- Часто возвращал неточные URL
- Тратились токены на генерацию

**СТАЛО:**
- source_url ВСЕГДА берется из `resource.url` (или `manufacturer.website_1`)
- LLM не тратит токены на генерацию URL

**Код (было):**
```python
source_url = news_item.get('source_url', '')
if not source_url and resource:
    source_url = resource.url
```

**Код (стало):**
```python
# source_url ВСЕГДА берем из ресурса
source_url = resource.url
```

**Экономия:** ~30 токенов/новость × 5 новостей × 220 ресурсов = **~33,000 токенов output** = **$0.01/запрос**

---

### 6️⃣ Оптимизация промптов для производителей (✅ реализовано)

**БЫЛО:**
```
Find all news about manufacturer {manufacturer_name} for the period from {start_date} to {end_date} inclusive.

Official manufacturer websites: {websites}

Use web search to find news. Look for all articles, publications, press releases, news about the manufacturer published on these websites or other industry sources for the specified period. For each found news item, find the title, news text (1-2 paragraphs) and source link.
```

**СТАЛО:**
```
Find news: {manufacturer_name}, {start_date} to {end_date}.

Official sites: {websites}

Use web search. **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

**Экономия:** ~150 токенов/производитель

---

## 📊 Итоговая экономия

| Оптимизация | Экономия токенов | Экономия $ | % снижения |
|------------|-----------------|-----------|------------|
| 1. Сокращение промптов | ~26,400 input | **$0.0026** | 28% input |
| 2. Минималистичный JSON | ~17,600 input | **$0.0018** | 18% input |
| 3. System промпты | ~6,600 input | **$0.0007** | 7% input |
| 4. Короткий summary | ~78,000 output | **$0.023** | 40% output |
| 5. Удаление source_url | ~33,000 output | **$0.01** | 17% output |
| **ИТОГО** | **~161,600** | **~$0.038** | **~29%** |

### 🎯 Результат

**Было:** ~$0.13 за поиск по 220 ресурсам  
**Стало:** ~$0.092 за поиск (**-29%**)

**В месяц (30 дней):**
- Было: ~$3.90/месяц
- Стало: **~$2.76/месяц**
- **Экономия: ~$1.14/месяц (~$13.68/год)**

---

## 📝 Примеры оптимизированных промптов

### Русский источник

```
Найди новости на https://avok.ru/news/ (АВОК), 15.01.2026 - 22.01.2026.

Используй веб-поиск. Для каждой новости: заголовок, краткое содержание (1 абзац).

JSON:
{"news": [{"title": "...", "summary": "..."}]}

Пусто: {"news": []}
```

### Английский источник

```
Find news on https://ashrae.org/news (ASHRAE), 2026-01-15 to 2026-01-22.

Use web search. For each: title, summary (1 paragraph). **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

### Производитель (с сайтами)

```
Find news: Carrier, 2026-01-15 to 2026-01-22.

Official sites: https://www.carrier.com, https://www.carrier.com/commercial/

Use web search. **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

### Производитель (без сайтов)

```
Find news: Daikin, 2026-01-15 to 2026-01-22.

Use web search. Sources: industry publications, press releases. **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

---

## 🔧 Технические детали

### Измененные файлы

**`hvac_news/news/discovery_service.py`:**
- Метод `_get_prompt_templates()` (строки ~356-430)
- Метод `_build_search_prompt()` (строки ~477-512)
- Метод `_build_manufacturer_search_prompt()` (строки ~1503-1535)
- Метод `_create_news_post()` (строки ~1001-1028)
- Метод `_create_manufacturer_news_post()` (строки ~1543-1571)
- System промпты для OpenAI, Grok, Anthropic (строки ~499, ~605, ~801)

### Ключевые изменения

1. **Промпты сокращены** с ~300 до ~100 токенов
2. **JSON формат** упрощен с ~100 до ~30 токенов
3. **System промпты** сокращены с ~40 до ~15 токенов
4. **Summary требование** изменено с "1-2 абзаца" на "1 абзац"
5. **source_url** больше не генерируется LLM (всегда из `resource.url`)

---

## 💡 Дополнительные возможности оптимизации (НЕ реализовано)

### Батчинг запросов (отложено)

**Идея:** Объединять несколько источников одного домена в один запрос.

**Пример:**
```
Find news on these sites, 2026-01-15 to 2026-01-22:
1. ashrae.org/news
2. ashrae.org/technical-resources/news
3. rehva.eu/news

Return JSON with site attribution.
```

**Потенциальная экономия:** ~30-40% input токенов

**Причина отложения:** Требует значительных изменений в архитектуре (группировка, обработка ответов, атрибуция).

---

## 📈 Мониторинг эффективности

Система отслеживает метрики через модель `DiscoveryAPICall`:
- `input_tokens` - количество input токенов
- `output_tokens` - количество output токенов
- `cost` - стоимость вызова
- `duration_ms` - время выполнения

**Запрос статистики:**
```python
from news.models import DiscoveryAPICall
from django.db.models import Sum, Avg

stats = DiscoveryAPICall.objects.filter(
    created_at__gte='2026-01-22'
).aggregate(
    total_input=Sum('input_tokens'),
    total_output=Sum('output_tokens'),
    total_cost=Sum('cost'),
    avg_duration=Avg('duration_ms')
)
```

---

## ✅ Чек-лист реализации

- [x] Сократить промпты - удалить избыточные фразы
- [x] Минималистичный JSON формат (убрать описания)
- [x] Оптимизировать system промпты
- [x] Сократить summary до 1 абзаца вместо 1-2
- [x] Удалить source_url из output (использовать resource.url)
- [x] Оптимизировать промпты для производителей
- [x] Обновить документацию

**Дата реализации:** 2026-01-22

---

## 📊 Сравнение с конкурентами

| Провайдер | До оптимизации | После оптимизации | Экономия |
|-----------|---------------|-------------------|----------|
| **Grok 4.1 Fast** | $0.13 | **$0.092** | **29%** |
| **OpenAI GPT-5.2** | $6.35 | ~$4.51 | 29% |
| **Claude Haiku 4.5** | $4.26 | ~$3.03 | 29% |
| **Claude Sonnet 4.5** | $8.36 | ~$5.94 | 29% |

**Вывод:** Оптимизация дает ~29% экономии для всех провайдеров.

---

**Последнее обновление:** 22 января 2026
