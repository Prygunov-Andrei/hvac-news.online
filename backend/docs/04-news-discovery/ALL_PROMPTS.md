# Все промпты системы HVAC News

Этот документ содержит все промпты, используемые в проекте для работы с LLM.

**📊 Версия:** 2.0 (оптимизированная, 2026-01-22)  
**🎯 Экономия:** ~29% стоимости по сравнению с v1.0

---

## 1. Промпты для поиска новостей по источникам

Система использует мультиязычные промпты — каждый промпт формируется на языке источника.

### 1.1. Русский промпт (для русскоязычных источников)

**Основная часть:**
```
Найди новости на {url} ({name}), {start_date} - {end_date}.

Используй веб-поиск. Для каждой новости: заголовок, краткое содержание (1 абзац).
```

**Формат ответа:**
```
JSON:
{"news": [{"title": "...", "summary": "..."}]}

Пусто: {"news": []}
```

---

### 1.2. Английский промпт (для англоязычных источников)

**Основная часть:**
```
Find news on {url} ({name}), {start_date} to {end_date}.

Use web search. For each: title, summary (1 paragraph). **Translate to Russian only.**
```

**Формат ответа:**
```
JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

---

### 1.3. Испанский промпт (для испаноязычных источников)

**Основная часть:**
```
Encuentra noticias en {url} ({name}), {start_date} a {end_date}.

Usa búsqueda web. Para cada: título, resumen (1 párrafo). **Traduce al ruso solamente.**
```

**Формат ответа:**
```
JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Vacío: {"news": []}
```

---

### 1.4. Немецкий промпт (для немецкоязычных источников)

**Основная часть:**
```
Finde Nachrichten auf {url} ({name}), {start_date} bis {end_date}.

Verwende Websuche. Für jede: Titel, Zusammenfassung (1 Absatz). **Übersetze nur auf Russisch.**
```

**Формат ответа:**
```
JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Leer: {"news": []}
```

---

### 1.5. Португальский промпт (для португалоязычных источников)

**Основная часть:**
```
Encontre notícias em {url} ({name}), {start_date} a {end_date}.

Use pesquisa web. Para cada: título, resumo (1 parágrafo). **Traduza apenas para russo.**
```

**Формат ответа:**
```
JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Vazio: {"news": []}
```

---

## 2. Промпты для поиска новостей по производителям

### 2.1. Если у производителя есть сайты

```
Find news: {manufacturer_name}, {start_date} to {end_date}.

Official sites: {website_1}, {website_2}, {website_3}

Use web search. **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

### 2.2. Если у производителя НЕТ сайтов

```
Find news: {manufacturer_name}, {start_date} to {end_date}.

Use web search. Sources: industry publications, press releases. **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

---

## 3. System промпты для разных провайдеров

### 3.1. OpenAI GPT

```
News search. Return JSON: {"news": [{"title": "...", "summary": "..."}]}
```

### 3.2. Grok (xAI)

```
News search with web. JSON: {"news": [{"title": "...", "summary": "..."}]}
```

### 3.3. Anthropic Claude

```
News search. JSON: {"news": [{"title": "...", "summary": "..."}]}
```

---

## 4. Промпты для перевода новостей

### 4.1. Промпт для перевода через OpenAI

**System промпт:**
```
You are a professional translator. Translate accurately while preserving all formatting.
```

**User промпт:**
```
Translate the following text from {source_language} to {target_language}. 
Preserve all HTML/Markdown formatting, links, and structure. 
Return only the translated text without any explanations or additional comments.

Text to translate:
{text}
```

**Параметры:**
- `source_language`: Russian, English, German, Portuguese
- `target_language`: Russian, English, German, Portuguese
- `temperature`: 0.3
- `max_tokens`: 2000

---

## 5. Кастомные промпты для hybrid источников

Для источников с типом `hybrid` администратор может задать свои кастомные инструкции в поле `custom_search_instructions`.

**Пример кастомного промпта:**

```
Этот источник публикует новости в формате PDF журналов на платформе issuu.com.
Используй веб-поиск для поиска новых публикаций за указанный период.
Если найдены новые журналы, извлеки из них основные новости HVAC-индустрии.

Период поиска: с {start_date} по {end_date} включительно.

{json_format}
```

---

## 6. Особенности формирования промптов

### 6.1. Определение языка и переводы

**✅ После оптимизации (v2.0, реализовано 2026-01-22):**

- Для **всех источников**: LLM возвращает только русский текст
- **source_url** не генерируется LLM (всегда берется из `resource.url`)
- **summary** сокращен до 1 абзаца (было: 1-2 абзаца)

**Формат ответа одинаков для всех языков:**
```json
{
  "news": [
    {
      "title": "Заголовок на русском",
      "summary": "Текст на русском (1 абзац)"
    }
  ]
}
```

**Переводы на другие языки (en, de, pt):**
- Создаются **отдельно** через `TranslationService`
- Только когда администратор публикует новость (меняет статус на `published`)
- Используется функция `publish_news_post()` из `news/services.py`

**Преимущества оптимизации:**
1. ⬇️ **Экономия токенов**: Промпты короче на ~40%
2. 💰 **Снижение стоимости**: ~29% экономии ($0.13 → $0.092 за поиск)
3. ⚡ **Быстрее**: Меньше токенов = быстрее ответ
4. 🎯 **Точность**: source_url всегда корректный (из базы данных)

---

## 7. Примеры реальных запросов (после оптимизации)

### Пример 1: Русский источник (АВОК)

**URL:** `https://avok.ru/news/`  
**Язык:** `ru`  
**Период:** 2026-01-15 до 2026-01-22

**Итоговый промпт:**
```
Найди новости на https://avok.ru/news/ (АВОК), 15.01.2026 - 22.01.2026.

Используй веб-поиск. Для каждой новости: заголовок, краткое содержание (1 абзац).

JSON:
{"news": [{"title": "...", "summary": "..."}]}

Пусто: {"news": []}
```

---

### Пример 2: Английский источник (ASHRAE)

**URL:** `https://www.ashrae.org/news`  
**Язык:** `en`  
**Период:** 2026-01-15 до 2026-01-22

**Итоговый промпт:**
```
Find news on https://www.ashrae.org/news (ASHRAE), 2026-01-15 to 2026-01-22.

Use web search. For each: title, summary (1 paragraph). **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

---

### Пример 3: Производитель (Carrier)

**Название:** Carrier  
**Сайты:** `https://www.carrier.com`, `https://www.carrier.com/commercial/`  
**Период:** 2026-01-15 до 2026-01-22

**Итоговый промпт:**
```
Find news: Carrier, 2026-01-15 to 2026-01-22.

Official sites: https://www.carrier.com, https://www.carrier.com/commercial/

Use web search. **Translate to Russian only.**

JSON:
{"news": [{"title": "русский", "summary": "русский текст"}]}

Empty: {"news": []}
```

---

## 8. Файл с промптами в коде

Все промпты находятся в файле:

```
hvac_news/news/discovery_service.py
```

**Метод формирования промптов:**
- `_get_prompt_templates(language)` — возвращает оптимизированные шаблоны (строки ~356-405)
- `_build_search_prompt(resource, start_date, end_date)` — формирует финальный промпт для источника (строки ~477-512)
- `_build_manufacturer_search_prompt(manufacturer, start_date, end_date)` — формирует промпт для производителя (строки ~1503-1535)

**Промпты для перевода:**
```
hvac_news/news/translation_service.py
```

Метод: `_translate_openai(text, source_lang, target_lang)`

---

## 9. Настройка промптов

### Через код

Редактируйте метод `_get_prompt_templates()` в `discovery_service.py`

### Через админку (для hybrid источников)

1. Откройте источник в Django Admin
2. Установите `source_type = 'hybrid'`
3. Заполните поле `custom_search_instructions`
4. Сохраните

Кастомные инструкции заменят стандартный промпт.

---

## 10. История оптимизаций

### Версия 1.0 (до 2026-01-22)

- Многословные промпты (~300 токенов)
- JSON с описаниями полей
- LLM генерировал source_url
- Summary требовал "1-2 абзаца"
- **Стоимость:** $0.13 за поиск по 220 ресурсам

### Версия 2.0 (2026-01-22) ✅ Текущая

- Короткие промпты (~100 токенов, **-67%**)
- Минималистичный JSON
- source_url из базы данных
- Summary 1 абзац (**-40% output**)
- **Стоимость:** $0.092 за поиск (**-29%**)

**Детали:** См. `PROMPT_COST_OPTIMIZATION.md`

---

**Последнее обновление:** 22 января 2026 (версия 2.0 - оптимизированная)

---

## Дополнительная документация

- `PROMPT_COST_OPTIMIZATION.md` - подробная информация об оптимизации стоимости
- `PROMPT_OPTIMIZATION_PLAN.md` - план реализации оптимизаций (v1.0 → v2.0)
- `NEWS_DISCOVERY_COST_ESTIMATE.md` - оценка затрат на поиск новостей


### 11.1. Проблема текущей реализации

**Для не-русских источников:**

1. LLM получает промпт на английском (или другом языке источника)
2. LLM переводит новости на 2 языка: оригинальный + русский
3. Система сохраняет обе версии (строки 1132-1135 в `discovery_service.py`)
4. **НО:** Оригинальная версия почти не используется!
5. Позже требуется отдельный перевод на de, pt, en через `TranslationService`

**Расход токенов:** ~2x output токенов (два языка вместо одного)

### 11.2. Рекомендуемое решение

**Шаг 1:** Изменить промпты для не-русских источников

**БЫЛО (текущий промпт для английских источников):**
```
Return the answer STRICTLY in JSON format:
{
  "news": [{
    "title": {
      "en": "News title in English",
      "ru": "Заголовок новости на русском"
    },
    "summary": {
      "en": "News text in English",
      "ru": "Текст новости на русском"
    },
    "source_url": "https://..."
  }]
}
```

**ДОЛЖНО БЫТЬ (оптимизированный промпт):**
```
Find all news on the website {url} ({name}) for the period from {start_date} to {end_date} inclusive.

Use web search to find news. Look for all articles, publications, press releases, news published on the website for the specified period. For each found news item, find the title, news text (1-2 paragraphs) and source link.

**IMPORTANT: Translate all news to Russian. Return only Russian text.**

Return the answer STRICTLY in JSON format:
{
  "news": [
    {
      "title": "Заголовок новости на русском",
      "summary": "Текст новости на русском языке (1-2 абзаца). Пиши новость напрямую, как журналист, от третьего лица.",
      "source_url": "https://example.com/news/article"
    }
  ]
}

If no news found, return: {"news": []}

Return ONLY JSON in Russian, without additional comments or explanations.
```

**Шаг 2:** Обновить код в `_create_news_post()` (строки 1100-1111)

Упростить логику - всегда ожидаем только русский текст:

```python
def _create_news_post(self, news_item: Dict, resource: NewsResource):
    """Создает новость из данных, полученных от LLM"""
    title_ru = news_item.get('title', 'Без заголовка')
    summary_ru = news_item.get('summary', '')
    source_url = news_item.get('source_url', '') or resource.url
    source_language = getattr(resource, 'language', 'en') or 'en'
    
    # Создаем новость (только русский текст)
    news_post = NewsPost.objects.create(
        title=title_ru,
        body=summary_ru,
        source_url=source_url,
        status='draft',
        source_language=source_language,
        author=self.user,
        pub_date=timezone.now()
    )
    
    # Переводы на другие языки будут добавлены позже через TranslationService
    # (когда администратор опубликует новость)
    news_post.save()
```

**Шаг 3:** Добавить автоматический перевод при публикации

В Django Admin или API, когда администратор меняет статус с `draft` на `published`:

```python
from news.translation_service import TranslationService

def publish_news(news_post):
    # Переводим на остальные языки
    translation_service = TranslationService()
    translations = translation_service.translate_news(
        title=news_post.title,
        body=news_post.body,
        source_lang='ru',
        target_languages=['en', 'de', 'pt']
    )
    
    # Сохраняем переводы
    for lang, trans in translations.items():
        setattr(news_post, f'title_{lang}', trans['title'])
        setattr(news_post, f'body_{lang}', trans['body'])
    
    news_post.status = 'published'
    news_post.save()
```

### 11.3. Экономия

**До оптимизации (220 источников, 50 не-русских с новостями):**
- Output: ~50 новостей × 2 языка × 150 токенов/новость = **15,000 токенов**
- Стоимость output (Grok): 15,000 × $0.15/1M = **$0.00225**

**После оптимизации:**
- Output: ~50 новостей × 1 язык × 150 токенов/новость = **7,500 токенов**
- Стоимость output (Grok): 7,500 × $0.15/1M = **$0.00113**

**Экономия:** ~50% на output токенах = ~$0.001 на каждый запуск (×365 дней = ~$0.36/год)

### 11.4. Реализация

**Файлы для изменения:**

1. `hvac_news/news/discovery_service.py`:
   - Метод `_get_prompt_templates()` (строки 356-491) - упростить промпты
   - Метод `_create_news_post()` (строки 1087-1138) - упростить обработку

2. `hvac_news/news/admin.py` или `hvac_news/news/views.py`:
   - Добавить автоматический перевод при публикации

3. Обновить тесты в `hvac_news/news/tests.py`

---

**Последнее обновление:** 22 января 2026 (версия 2.0 - оптимизированная)

---

## Дополнительная документация

- `PROMPT_COST_OPTIMIZATION.md` - подробная информация об оптимизации стоимости
- `PROMPT_OPTIMIZATION_PLAN.md` - план реализации оптимизаций (v1.0 → v2.0)
- `NEWS_DISCOVERY_COST_ESTIMATE.md` - оценка затрат на поиск новостей

