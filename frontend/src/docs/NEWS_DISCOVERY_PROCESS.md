# Процесс автоматического поиска новостей

## Общая схема

```
[ResourcesPage] → [NewsDiscoveryDialog] → [Backend API] → [LLM API] → [Создание новостей]
                         ↓
              [NewsDiscoveryProgress]
                         ↓
                  [Polling статуса]
```

---

## Детальное описание процесса

### 1. **Инициация (ResourcesPage.tsx)**

**Где:** Страница "Новостные ресурсы" (`/resources`)

**Триггер:** Кнопка "✨ Найти новости" (видна только администраторам)

**Условия отображения:**
- Пользователь авторизован как администратор (`user.is_staff === true`)
- Список ресурсов успешно загружен
- Есть хотя бы один ресурс в базе данных

**Код:**
```tsx
{isAdmin && !loading && !error && resources.length > 0 && (
  <Button onClick={() => setDiscoveryDialogOpen(true)}>
    <Sparkles className="w-4 h-4" />
    Найти новости
  </Button>
)}
```

---

### 2. **Стадия подтверждения (NewsDiscoveryDialog - Confirm)**

**Что показывается:**

1. **Последний поиск:**
   - Дата и время последнего запуска поиска новостей
   - Если никогда не запускался → "Никогда"
   - Загружается через API: `GET /admin/references/newsresource/discover-news-info/`

2. **Период поиска:**
   - Временной диапазон для поиска новостей
   - Например: "с 15 декабря 2024 г. по 7 января 2026 г."
   - Определяется бэкендом на основе даты последнего поиска

3. **Количество источников:**
   - Сколько новостных ресурсов будет обработано
   - Передается из ResourcesPage (`resources.length`)

4. **Предупреждение:**
   > ⚠️ **Важно:** Поиск может занять несколько минут. Все найденные новости будут сохранены как черновики для последующего редактирования.

**Действия пользователя:**
- **Отмена** → закрывает диалог
- **Начать поиск** → переход к стадии прогресса

**Код запроса:**
```tsx
const handleStartDiscovery = async () => {
  const response = await referencesService.startNewsDiscovery();
  setStage('progress');
  toast.success('Поиск новостей запущен!');
};
```

---

### 3. **Запуск поиска (Backend API)**

**Endpoint:** `POST /admin/references/newsresource/discover-news/`

**Заголовки:**
```http
Authorization: Bearer {JWT_TOKEN}
Accept-Language: ru
X-Requested-With: XMLHttpRequest
Content-Type: application/json
```

**Тело запроса:** `{}` (пустой объект)

**Что происходит на бэкенде:**

1. **Получение списка ресурсов:**
   - Выбираются все активные новостные ресурсы из базы данных
   - Для каждого ресурса определяется период поиска (с момента последнего поиска)

2. **Вызов LLM API:**
   - Для каждого ресурса отправляется запрос к LLM (например, OpenAI GPT)
   - LLM анализирует RSS-ленту или веб-страницу источника
   - LLM извлекает новые статьи и суммаризирует их

3. **Создание черновиков:**
   - Каждая найденная новость создается как запись в базе данных
   - Статус: `draft` (черновик)
   - Заполняются поля:
     - `title` (заголовок)
     - `body` (HTML-содержимое)
     - `source_language` (язык источника)
     - `source_url` (ссылка на оригинал)
     - `pub_date` (дата публикации)
     - `is_no_news_found` (false для реальных новостей, true если новостей не найдено)

4. **Обработка случаев "новостей не найдено":**
   - Если LLM не нашел новостей в источнике, создается специальная запись:
     - `is_no_news_found = true`
     - `title = "Новостей от источника '{название}' не найдено"`
     - `body = пустое`
   - Эти записи можно потом массово удалить через `/news/not-found`

5. **Обновление статуса:**
   - Backend сохраняет статус выполнения в Redis/кэше
   - Обновляет процент выполнения, количество обработанных ресурсов

**Ответ:**
```json
{
  "status": "running",
  "processed": 0,
  "total": 15,
  "percent": 0,
  "message": "Поиск новостей запущен"
}
```

---

### 4. **Стадия прогресса (NewsDiscoveryProgress)**

**Компонент:** `NewsDiscoveryProgress`

**Что происходит:**

1. **Инициализация polling:**
   ```tsx
   useEffect(() => {
     pollStatus(); // Первый запрос сразу
     // Затем каждые 3 секунды
     setInterval(pollStatus, 3000);
   }, []);
   ```

2. **Запрос статуса:**
   - **Endpoint:** `GET /admin/references/newsresource/discover-news-status/`
   - **Частота:** каждые 3 секунды
   - **Ответ:**
     ```json
     {
       "status": "running",
       "processed": 5,
       "total": 15,
       "percent": 33,
       "created": 12,
       "errors": 1,
       "message": "Обработано 5 из 15 источников"
     }
     ```

3. **Отображение прогресса:**
   - Прогресс-бар с процентом выполнения
   - Текст: "Обработано X из Y источников"
   - Анимированная иконка загрузки (вращающийся спиннер)
   - Статус: "Выполняется..."

4. **Обработка ошибок:**
   - Если `status === 'error'`:
     - Остановка polling
     - Отображение сообщения об ошибке
     - Вызов `onError(errorMessage)`

5. **Завершение:**
   - Если `status === 'completed'`:
     - Остановка polling
     - Вызов `onComplete(status)`
     - Переход к стадии результатов

**Визуальное представление:**
```
┌─────────────────────────────────────┐
│ 🔄 Поиск новостей      [Выполняется] │
├─────────────────────────────────────┤
│                                     │
│ Обработано 8 из 15 источников  53%  │
│ ████████████░░░░░░░░░░░░░           │
│                                     │
└─────────────────────────────────────┘
```

---

### 5. **Стадия результатов (NewsDiscoveryDialog - Results)**

**Что показывается:**

1. **Метрики:**
   ```
   ┌────────────────┬────────────────┬────────────────┐
   │ Создано        │ Ошибок         │ Обработано     │
   │ новостей       │                │                │
   │      12        │       1        │       15       │
   └────────────────┴────────────────┴────────────────┘
   ```

2. **Информационное сообщение:**
   > ℹ️ Все новости сохранены как **черновики**. Вы можете просмотреть и отредактировать их на странице новостей, используя фильтр "Черновики".

**Действия пользователя:**
- **Вернуться к источникам** → закрывает диалог, остается на странице ресурсов
- **Просмотреть новости** → переход на `/news` (список новостей)

---

## Технические детали API

### API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/admin/references/newsresource/discover-news-info/` | Получить информацию о последнем поиске |
| `POST` | `/admin/references/newsresource/discover-news/` | Запустить поиск новостей |
| `GET` | `/admin/references/newsresource/discover-news-status/` | Получить статус текущего поиска |

### Типы данных

**NewsDiscoveryInfo:**
```typescript
interface NewsDiscoveryInfo {
  last_discovery_date: string | null;  // Дата последнего поиска
  period_start: string | null;         // Начало периода поиска
  period_end: string;                  // Конец периода поиска
  total_resources: number;             // Общее количество ресурсов
}
```

**NewsDiscoveryStatus:**
```typescript
interface NewsDiscoveryStatus {
  status: 'running' | 'completed' | 'error';  // Статус выполнения
  processed: number;                          // Обработано ресурсов
  total: number;                              // Всего ресурсов
  percent: number;                            // Процент выполнения (0-100)
  created?: number;                           // Создано новостей
  errors?: number;                            // Количество ошибок
  total_processed?: number;                   // Альтернатива для processed
  message?: string;                           // Сообщение (при ошибке)
}
```

---

## Авторизация

**Требования:**
- Пользователь должен быть администратором (`is_staff = true`)
- JWT токен должен быть валидным
- Токен передается в заголовке: `Authorization: Bearer {token}`

**Проверки:**
```tsx
const isAdmin = user?.is_staff === true;

// Кнопка видна только админам
{isAdmin && <Button>Найти новости</Button>}

// При открытии диалога логируются данные авторизации
console.log('👤 Пользователь:', {
  email: user?.email,
  is_staff: user?.is_staff,
  is_authenticated: !!user,
});
console.log('🔑 Токен:', localStorage.getItem('access_token') ? 'ПРИСУТСТВУЕТ' : 'ОТСУТСТВУЕТ');
```

---

## Обработка ошибок

### Возможные ошибки:

1. **401 Unauthorized:**
   - Токен отсутствует или невалиден
   - Пользователь не авторизован
   - **Решение:** Перелогиниться

2. **403 Forbidden:**
   - Пользователь не является администратором
   - **Решение:** Доступ только для админов

3. **500 Internal Server Error:**
   - Ошибка на стороне бэкенда
   - LLM API недоступен
   - **Решение:** Проверить логи Django, доступность LLM API

4. **Network Error:**
   - Отсутствует соединение с сервером
   - Timeout запроса
   - **Решение:** Проверить подключение к интернету, доступность бэкенда

### Отображение ошибок:

```tsx
// В стадии подтверждения
{error && (
  <Alert variant="destructive">
    <AlertCircle className="h-4 w-4" />
    <AlertDescription>{error}</AlertDescription>
  </Alert>
)}

// Также показывается toast
toast.error(`Ошибка: ${errorMsg}`);
```

---

## Поведение пользовательского интерфейса

### Состояния диалога:

1. **Confirm (подтверждение):**
   - Показывается информация о поиске
   - Кнопки: "Отмена" | "Начать поиск"
   - Состояние loading для кнопки при запуске

2. **Progress (прогресс):**
   - Прогресс-бар
   - Обновление каждые 3 секунды
   - Нельзя закрыть диалог (нет кнопки закрытия)
   - Описание: "Пожалуйста, не закрывайте это окно. Поиск выполняется..."

3. **Results (результаты):**
   - Метрики с цветными карточками
   - Кнопки: "Вернуться к источникам" | "Просмотреть новости"
   - Можно закрыть диалог

### Toast уведомления:

- ✅ "Поиск новостей запущен!" (при старте)
- ✅ "Поиск завершен! Создано новостей: X" (при завершении)
- ❌ "Ошибка: {сообщение}" (при ошибке)

---

## Производительность и оптимизация

1. **Polling интервал:** 3 секунды
   - Баланс между актуальностью данных и нагрузкой на сервер

2. **Очистка интервала:**
   - При размонтировании компонента
   - При завершении или ошибке поиска
   - Предотвращает утечки памяти

3. **Обработка единичных ошибок polling:**
   - Если один запрос статуса упал → продолжаем опрос
   - Только критические ошибки прерывают процесс

4. **Асинхронность:**
   - Поиск выполняется в фоновом режиме (background task)
   - Не блокирует работу с другими разделами приложения

---

## Примеры использования

### Сценарий 1: Успешный поиск

1. Администратор открывает `/resources`
2. Видит 15 новостных ресурсов
3. Нажимает "✨ Найти новости"
4. Видит: "Последний поиск: 5 января 2026 г., 14:30"
5. Видит: "Период поиска: с 5 января 2026 г. по 7 января 2026 г."
6. Нажимает "Начать поиск"
7. Видит прогресс-бар: 0% → 20% → 40% → 60% → 80% → 100%
8. Видит результаты:
   - Создано новостей: 12
   - Ошибок: 1
   - Обработано: 15
9. Нажимает "Просмотреть новости"
10. Переходит на `/news`, видит новые черновики

### Сценарий 2: Новостей не найдено

1. LLM API анализирует источник
2. Не находит новых публикаций за период
3. Создается запись с `is_no_news_found = true`
4. Заголовок: "Новостей от источника 'HVAC Magazine' не найдено"
5. Администратор может:
   - Открыть `/news/not-found`
   - Просмотреть все записи "не найдено"
   - Проверить источники вручную (кнопка "Проверить источник")
   - Массово удалить записи

### Сценарий 3: Ошибка при поиске

1. Администратор запускает поиск
2. На 5-м ресурсе происходит ошибка (LLM API timeout)
3. Backend продолжает обработку остальных
4. Счетчик ошибок увеличивается: `errors: 1`
5. Остальные 10 ресурсов обрабатываются успешно
6. Результаты:
   - Создано новостей: 8
   - Ошибок: 1
   - Обработано: 15
7. Администратор видит неполные результаты, но процесс завершился

---

## Связь с другими функциями

### После создания новостей:

1. **Просмотр черновиков:** `/drafts` или фильтр "Черновики" в `/news`
2. **Редактирование:** Клик на новость → `/news/edit/{id}`
3. **Публикация:** Изменить статус с `draft` на `published`
4. **Удаление записей "не найдено":** `/news/not-found` → массовое удаление

### Автоматические действия:

1. **Автоперевод:** Если включено, новости автоматически переводятся на все языки
2. **Обработка изображений:** URL изображений преобразуются в полные пути
3. **Сохранение метаданных:** `source_url`, `source_language`, `created_at`

---

## Диаграмма последовательности

```
Пользователь          Frontend                  Backend                    LLM API
    |                     |                         |                         |
    |--[Клик "Найти"]---->|                         |                         |
    |                     |--[POST discover]------->|                         |
    |                     |<---[status:running]-----|                         |
    |                     |                         |                         |
    |                     |                         |--[Запрос RSS]---------->|
    |                     |                         |<--[Анализ контента]-----|
    |                     |                         |                         |
    |                     |--[GET status]---------->|                         |
    |                     |<---[processed:5/15]-----|                         |
    |<--[Прогресс 33%]----|                         |                         |
    |                     |                         |                         |
    |                     |--[GET status]---------->|                         |
    |                     |<---[processed:10/15]----|                         |
    |<--[Прогресс 66%]----|                         |                         |
    |                     |                         |                         |
    |                     |--[GET status]---------->|                         |
    |                     |<---[completed]----------|                         |
    |<--[Результаты]------|                         |                         |
    |                     |                         |                         |
```

---

## Заключение

Процесс автоматического поиска новостей — это сложный многоступенчатый workflow, который включает:

✅ **UI/UX:** Интуитивный интерфейс с трехстадийным диалогом  
✅ **Backend:** Асинхронная обработка ресурсов с отслеживанием прогресса  
✅ **LLM Integration:** Интеграция с AI для анализа и суммаризации новостей  
✅ **Error Handling:** Надежная обработка ошибок на всех уровнях  
✅ **Real-time Updates:** Обновление прогресса каждые 3 секунды  
✅ **Admin Control:** Полный контроль администратора над процессом

Все созданные новости сохраняются как черновики, что позволяет администратору проверить и отредактировать их перед публикацией.
