# Инфографика статистики источников новостей

## Описание

Дашборд статистики предоставляет визуальный обзор эффективности и активности источников новостей в системе. Он помогает:
- Быстро оценить состояние системы поиска новостей
- Выявить наиболее продуктивные источники
- Выявить проблемные и неактивные источники
- Мониторить эффективность работы системы

## Расположение

Дашборд отображается на странице **"Новостные ресурсы"** (`/resources`) **перед таблицей источников** и доступен **только для администраторов**.

## Структура дашборда

### 1. Основные метрики (Overview Cards)

Шесть карточек с ключевыми показателями:

#### 1.1 Всего источников
- **Значение:** Общее количество источников в системе
- **Дополнительно:** Количество источников со статистикой
- **Цвет:** Синий
- **Иконка:** 📚 BookOpen

#### 1.2 Активных источников
- **Значение:** Количество активных источников
- **Дополнительно:** Процент от общего числа
- **Цвет:** Зеленый
- **Иконка:** 📈 TrendingUp

#### 1.3 Всего найдено новостей
- **Значение:** Общее количество найденных новостей
- **Дополнительно:** Количество поисковых запросов
- **Цвет:** Зеленый
- **Иконка:** 📚 BookOpen

#### 1.4 Новостей за 30 дней
- **Значение:** Количество новостей, найденных за последние 30 дней
- **Дополнительно:** Процент от общего числа
- **Цвет:** Оранжевый
- **Иконка:** 📅 Calendar

#### 1.5 Процент успешности
- **Значение:** Процент успешных поисков (поиски с найденными новостями)
- **Дополнительно:** Среднее количество новостей за поиск
- **Цвет:** 
  - Зеленый ≥ 70%
  - Оранжевый 50-69%
  - Красный < 50%
- **Иконка:** 📊 BarChart3

#### 1.6 Средний рейтинг
- **Значение:** Средний рейтинг всех источников
- **Дополнительно:** "Оценка продуктивности"
- **Цвет:** Желтый
- **Иконка:** ⭐ Star

### 2. Распределение по категориям

Круговая диаграмма (Pie Chart) с четырьмя категориями:

#### Категории источников:

1. **Высокопродуктивные** (зеленый)
   - Рейтинг ≥ 50
   - Источники с высокой активностью и большим количеством новостей

2. **Средние** (желтый)
   - Рейтинг 20-49
   - Источники со средней активностью

3. **Низкопродуктивные** (красный)
   - Рейтинг 1-19
   - Источники с низкой активностью

4. **Проблемные** (темно-красный)
   - Рейтинг = 0
   - Источники с ошибками или без найденных новостей

**Возможности:**
- Hover для просмотра точного количества источников
- Проценты для каждой категории
- Легенда с описанием категорий

### 3. Топ источников

Три таблицы с топ-10 источников по разным критериям:

#### 3.1 По количеству новостей
- **Колонки:**
  - № (ранг с иконками для топ-3)
  - Название источника
  - Всего новостей
  - Рейтинг (число + цветной индикатор)

#### 3.2 По рейтингу
- **Колонки:**
  - № (ранг с иконками для топ-3)
  - Название источника
  - Рейтинг (число + цветной индикатор)

#### 3.3 По активности (30 дней)
- **Колонки:**
  - № (ранг с иконками для топ-3)
  - Название источника
  - Новостей за 30 дней
  - Рейтинг (число + цветной индикатор)

**Иконки топ-3:**
- 🥇 1-е место: Trophy (золотой)
- 🥈 2-е место: Medal (серебряный)
- 🥉 3-е место: Award (бронзовый)

**Цветовая индикация рейтинга:**
- Зеленый градиент: рейтинг ≥ 50
- Желтый градиент: рейтинг 20-49
- Красный градиент: рейтинг < 20

### 4. Дополнительная информация

**Предупреждение об ошибках** (отображается если есть ошибки):
- Красная карточка с информацией о количестве ошибок
- Рекомендация проверить проблемные источники

## API Endpoint

### GET /api/references/resources/statistics_summary/

**Ответ:**
```json
{
  "overview": {
    "total_resources": 220,
    "resources_with_stats": 150,
    "active_resources": 120,
    "inactive_resources": 100
  },
  "aggregated": {
    "total_news_found": 5000,
    "total_searches": 3000,
    "total_no_news": 800,
    "total_errors": 50,
    "news_last_30_days": 450
  },
  "averages": {
    "success_rate": 71.5,
    "avg_news_per_search": 1.67,
    "avg_ranking_score": 35.2
  },
  "categories": {
    "high_performers": 25,
    "medium_performers": 80,
    "low_performers": 45,
    "problematic": 5
  },
  "top_sources": {
    "by_news": [
      {
        "id": 1,
        "name": "HVAC News Daily",
        "total_news": 450,
        "ranking_score": 85.5
      },
      ...
    ],
    "by_ranking": [...],
    "by_activity": [...]
  }
}
```

## Компоненты

### Структура файлов:

```
/components/statistics/
├── MetricCard.tsx              # Карточка с метрикой
├── CategoryChart.tsx           # Круговая диаграмма категорий
├── TopSourcesTable.tsx         # Таблица топ-источников
└── SourceStatisticsDashboard.tsx  # Основной компонент дашборда
```

### MetricCard

Карточка для отображения одной метрики.

**Props:**
- `title` - название метрики
- `value` - основное значение (число или строка)
- `subtitle` - дополнительная информация
- `icon` - иконка из lucide-react
- `color` - цвет (blue, green, orange, red, purple, yellow)
- `onClick` - обработчик клика (опционально)

**Пример:**
```tsx
<MetricCard
  title="Всего источников"
  value={220}
  subtitle="150 со статистикой"
  icon={BookOpen}
  color="blue"
/>
```

### CategoryChart

Круговая диаграмма для распределения по категориям.

**Props:**
- `highPerformers` - количество высокопродуктивных
- `mediumPerformers` - количество средних
- `lowPerformers` - количество низкопродуктивных
- `problematic` - количество проблемных

**Пример:**
```tsx
<CategoryChart
  highPerformers={25}
  mediumPerformers={80}
  lowPerformers={45}
  problematic={5}
/>
```

### TopSourcesTable

Таблица топ-источников.

**Props:**
- `title` - заголовок таблицы
- `data` - массив TopSource[]
- `type` - тип таблицы ('news' | 'ranking' | 'activity')

**Пример:**
```tsx
<TopSourcesTable
  title="По количеству новостей"
  data={topSources.by_news}
  type="news"
/>
```

### SourceStatisticsDashboard

Основной компонент, объединяющий все части дашборда.

**Функционал:**
- Автоматическая загрузка статистики при монтировании
- Обработка состояний загрузки и ошибок
- Кнопка обновления данных
- Форматирование чисел и процентов
- Динамическая цветовая индикация

## Использование

### В ResourcesPage:

```tsx
import SourceStatisticsDashboard from '../components/statistics/SourceStatisticsDashboard';

// В компоненте:
{!loading && !error && resources.length > 0 && (
  <div className="space-y-8">
    {/* Дашборд статистики - только для админов */}
    {isAdmin && (
      <SourceStatisticsDashboard />
    )}
    
    {/* Остальное содержимое */}
  </div>
)}
```

## Обработка ошибок

### 1. Недоступность API
- Отображается компонент `ApiErrorBanner`
- Кнопка "Повторить" для повторной загрузки

### 2. Отсутствие данных
- Показывается сообщение "Нет данных для отображения"
- Кнопка "Загрузить статистику"

### 3. Загрузка
- Индикатор загрузки с анимацией
- Сообщение "Загрузка статистики..."

## Адаптивность

### Desktop (≥1024px)
- Метрики: 3 колонки (grid-cols-3)
- Топ-таблицы: 3 колонки рядом

### Tablet (768-1023px)
- Метрики: 2 колонки (grid-cols-2)
- Топ-таблицы: 1 колонка (stack)

### Mobile (<768px)
- Метрики: 1 колонка (grid-cols-1)
- Топ-таблицы: 1 колонка
- Горизонтальная прокрутка таблиц

## Производительность

### Оптимизации:
1. **Загрузка:** Данные загружаются один раз при открытии страницы
2. **Кэширование:** Нет автоматического обновления (только по клику)
3. **Рендеринг:** Условный рендеринг компонентов
4. **Recharts:** Ленивый рендеринг диаграмм

### Рекомендации:
- Не загружать статистику слишком часто (нагрузка на БД)
- Использовать кэширование на backend (Redis)
- Обновлять статистику только при необходимости

## Расчет метрик (на backend)

### Рейтинг источника (ranking_score):
```python
ranking_score = (
    total_news_found * 2 +      # Вес количества новостей
    news_last_30_days * 3 +      # Вес свежих новостей
    success_rate * 0.5 -         # Вес успешности
    total_errors * 5             # Штраф за ошибки
)
```

### Процент успешности (success_rate):
```python
success_rate = (total_searches - total_no_news - total_errors) / total_searches * 100
```

### Категории:
- **Высокопродуктивные:** ranking_score >= 50
- **Средние:** 20 <= ranking_score < 50
- **Низкопродуктивные:** 0 < ranking_score < 20
- **Проблемные:** ranking_score == 0

## Будущие улучшения

### Планируется:
1. **Графики трендов:**
   - График активности за последние 30/90 дней
   - Динамика рейтинга источников

2. **Фильтрация:**
   - Клик по категории → фильтр таблицы источников
   - Клик по топ-источнику → переход к деталям

3. **Экспорт:**
   - Экспорт статистики в CSV/Excel
   - Генерация PDF отчетов

4. **Сравнение:**
   - Сравнение периодов (месяц к месяцу)
   - Топ растущие/падающие источники

5. **Уведомления:**
   - Оповещения о проблемных источниках
   - Еженедельные email-отчеты

## Тестирование

### Проверка отображения:
1. Авторизуйтесь как администратор
2. Перейдите на страницу `/resources`
3. Дашборд должен отобразиться перед таблицей источников

### Проверка данных:
1. Убедитесь, что все метрики отображаются корректно
2. Проверьте, что диаграмма содержит все категории
3. Убедитесь, что топ-таблицы заполнены

### Проверка обработки ошибок:
1. Отключите backend
2. Проверьте отображение ApiErrorBanner
3. Проверьте работу кнопки "Повторить"

## Логирование

Все операции логируются в консоль:

```
📊 Статистика загружена: {...}
❌ Ошибка загрузки статистики: {...}
```

## Итог

Дашборд статистики источников предоставляет:
- ✅ Визуальный обзор всей системы
- ✅ Быструю идентификацию проблем
- ✅ Мониторинг эффективности источников
- ✅ Данные для принятия решений
- ✅ Удобный интерфейс для администраторов
